class Solution {
    public boolean stoneGame(int[] piles) {
        //as question want that alice should win whoever will start first and play optimaaly than first person will only win
        return true;
        
    }
}